---
session_id: 887fe5e4-777e-4909-8cda-8f3e7456ccee
date: 2026-07-27
project: claude-harness
type: dev
goal: fix UserPromptSubmit 5s timeout → escalated to full qmd + embedding-stack repair
status: COMPLETE — bge-m3 embed done (75380 vectors, 0 pending), retrieval verified, articles purged
---

# Session 887fe5e4 — hook timeout → qmd/embed stack repair

## Did
- Diagnosed UserPromptSubmit 5s timeout: root cause was the skill-retrieval hook (added 2d prior) running `qmd query` per prompt against a degraded 1.2GB index (85% unembedded). NOT the brew upgrade (red herring — chased node/ggml rollbacks for ~6h before proving qmd bundles its own ggml/llama, linking only Apple system libs).
- Fixed hook: outer timeout 5→10s, internal 5→3s, pause-sentinel (`~/.cache/qmd/.skill-hook-paused`) for embed runs.
- Upgraded qmd 1.0.7 → 2.5.3 (5mo behind; fixed QMD_EMBED_MODEL bug #552, embed retry/stability, SQLite busy_timeout).
- Embedding model swap: embeddinggemma-300M → Qwen3-Embedding-0.6B → **bge-m3**. Qwen3 GGUF deadlocks bulk embed (2025 llama.cpp eval bug, unfixed in node-llama-cpp 3.18.1 / build b8390 — event loop parks in kevent, native completion callback never fires). bge-m3 embeds cleanly.
- Full brew + npm upgrade (~65 pkgs). mise + node@25 (node version proved irrelevant to the deadlock). npm `allow-scripts` set (npm 12 blocks native-addon rebuilds by default).
- Telemetry check: skill-retrieval fix verified (5/5 ok, 0 timeouts); memory-infer had a JSON parse bug (GLM trailing prose → "Extra data" error, ~5% of runs) — FIXED with tolerant `first_json_obj()` parser; warm-start FATals were transient (this session's upheaval); strengthened recall SKILL.md description with explicit triggers.

## Decisions
- **bge-m3 over Qwen3-Embedding-0.6B**: Qwen3 GGUF sustained-eval deadlock in llama.cpp; bge-m3 reliable + near-top of practical small-model class. Quality gap vs Qwen3 is ~2-4 MTEB pts, imperceptible for personal-notes /recall.
- Keep node 25 via mise (not 26): irrelevant to embed; stable. Bump to 26 post-embed if desired.
- npm allow-scripts: required for native-addon rebuilds under npm 12 default-block.
- Hook pause-sentinel: prevents skill-retrieval query contention during qmd embed (the original concurrency theory was wrong, but the sentinel is still good hygiene).

## State now (FINAL — session complete)
- **bge-m3 embed COMPLETE**: 75,380 vectors / 2,742 docs / 0 pending. Loop finished 18:02 IST; retrieval verified (`qmd query` → this session's transcript @ 93% score). ~2–4% chunks failed (edge-case model errors); docs marked embedded, ~96%+ coverage. DB ~487 MB.
- **articles collection PURGED** (user decision): `qmd collection remove articles` deleted 1805 docs / 4.4 GB of newsletter scrapes — not needed for `/recall`, was the embed long-tail. DB 1.2 GB → 487 MB.
- Qwen3 model deleted (610 MB reclaimed). Models on disk: bge-m3 (embed) + qwen3-reranker + query-expansion — all active, nothing stale.
- Sentinel removed → skill-retrieval hook **re-enabled and healthy**. Monitoring cron deleted (embed done).
- node 25 (mise) · qmd 2.5.3 · bge-m3 in `~/.config/qmd/index.yml`.
- memory-infer parse bug FIXED (`first_json_obj()`). Key/account/glm-4.7 verified working (NOT a credits issue — earlier misdiagnosis from wrong endpoint).

## Open threads (remaining)
- [x] bge-m3 embed COMPLETE; sentinel removed; `/recall` retrieval verified; Qwen3 cache deleted.
- [x] Repo changes committed (`a47b886` + `f01d549`): settings.adopter.json timeout, skill-retrieval-hook.sh (timeout+sentinel), recall/SKILL.md triggers, memory-infer-hook.sh (parse fix), handoff + PROJECT_LOG.
- Still uncommitted (outside repo, by design — deployed/live configs): `~/.claude/settings.json`, `~/.config/qmd/index.yml`, `~/.zshrc` (mise).
- 3 brew stragglers (`libnghttp3`, `mise`, `blackhole-2ch`) + npm 11.12→11.18 — optional, batch anytime.
- node 25 vs 26: optional user choice (both work; 25 is current via mise).
- ~2–4% chunks failed embedding (edge-case model errors on some session docs) — if `/recall` ever feels gappy on a specific session, re-run `qmd embed` to retry the stragglers.

## Artifacts
- Commits: `a47b886` (vault-push: hook fix + qmd 2.5.3 + bge-m3) + `f01d549` (memory-infer parse fix) + this push.
- `/tmp/qmd-embed-full.sh` — multi-pass loop (kept for reference; reusable for future re-embeds; has `-f` on pass 1 for clean rebuilds).
- Sentinel + monitoring cron: removed (embed complete).
