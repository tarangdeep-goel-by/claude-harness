---
session_id: a8b1ae19-cb07-4954-a39f-b015d25958cf
date: 2026-07-26
project: claude-harness
type: dev
goal: "claude-harness zip → dev-focused public v0.4.0 release; ship Hermes mechanism #2 (skill-retrieval)."
status: shipped (public, #2 live); rebuild-confirmation + sync-decision pending
---

# Session a8b1ae19 — harness v0.4.0 public release + skill-retrieval (#2)

## Did
- **Dev-focused fork v0.4.0**: stripped SM-coupled skills (metabase/mixpanel/mp-to-ph-migration/
  sm-design-system/play-console/capture-journey/flutter-dev) + SM refs; brought the workflow-engine
  family (workflow-engine, task-triage, debug-escalation, done-gate, review-merge, karpathy-guidelines)
  into the repo, all live-symlinked (drift-free, repo = single source of truth). Added drawio + humanizer.
- **Portability + privacy**: hardcoded `/Users/tarang` → session-cwd-derived slugs across the memory
  hooks + scripts; distilled `CLAUDE.md` into a clean operating-discipline doc; removed the entire
  telemetry-upload pipeline (setup-telemetry-sink/collate-telemetry/collect_telemetry + Drive utilities)
  + gog/rclone deps — all telemetry stays local.
- **Public release**: `git filter-repo` purged `plans/adopt-dev-focused.md` (personal info) from all
  history + `--force-with-lease` push to origin; added MIT LICENSE, README polish, a generic
  pre-commit (SM drift-patterns stripped); flipped PRIVATE→PUBLIC; set description + topics;
  deleted stale `feat/memory-infra`. Now live at github.com/tarangdeep-goel-by/claude-harness.
- **Hermes mechanism #2 shipped**: `vault-scripts/skill-retrieval-hook.sh` (UserPromptSubmit) runs
  `qmd query <prompt> -c skills -n 3 --json` over descriptions staged by `vault-scripts/build-skill-index.sh`
  (mtime-guarded, idempotent); injects top-3 score ≥0.5 as `additionalContext`, silent below. Wired
  into `settings.adopter.json` + `install.sh` + `warm-start.sh` (qmd mutex block) + the live
  `~/.claude/settings.json`. Plan: `plans/vast-splashing-wirth.md`.
- **4 bugs fixed during build**: macOS has no `timeout` (detect `gtimeout`/fall-back); `python3 - <<PY`
  reads the script from stdin so pass JSON via env not a pipe; re-added `import sys`; qmd's
  `--min-score` filters on an internal score inconsistently (dropped it, filter in python).

## Decisions
- Repo is **dev-focused, not SM-centric**. skill-retrieval REPLACES skill-router in adopter settings
  (SM skills stripped → skill-router inert; it stays in vault-scripts for owners/vault-work).
- Index skill `description` only (staging dir, not full SKILL.md bodies); threshold 0.5 default
  (`SKILL_RETRIEVAL_MIN_SCORE` env); top-3; observe-only (exit 0 always).
- Repo made PUBLIC. `--force-with-lease` (not bare `--force`) is the harness-sanctioned force-push
  form (block-dangerous-hook allows it).

## State now
- `origin/main` = `bb4984d`. Repo public, MIT, single-branch (main), 17 skills, enhanced hooks wired
  in `settings.adopter.json`, hooks-smoke green.
- Live `~/.claude/settings.json` has the UserPromptSubmit skill-retrieval hook; both new scripts
  symlinked into `~/vault/scripts/`; live `skills` qmd collection built (23 skills).
- Pre-purge history backup: `/tmp/claude-harness-pre-purge.bundle` (+ `/tmp/adopt-dev-focused-plan-backup.md`).

## Open threads
- **Confirm skill-retrieval end-to-end**: a `qmd embed -f` was killed mid-run (may have half-done
  embeddings); `build-skill-index.sh` rebuild was in flight at session end. Re-verify: `qmd query
  "fix failing test" -c skills` returns debug-escalation, and the live hook emits for a debug prompt.
- **Sync architecture (pending user)**: Option A = split personal config → `~/.claude/settings.local.json`
  + symlink `settings.json` → repo `settings.adopter.json` + a SessionStart re-link guard (true
  bidirectional auto-sync); vs leave-as-is (manual per-hook add to live settings).
- **Hermes #1** (autonomous skill-distillation Stop hook) — deferred, separate plan; compounds with #2.

## Artifacts
- Commits: `1e15739` (v0.4.0) · `048709b`/`4a3e3e7` (LICENSE/README/generic pre-commit) · `d8836ae`
  (skill-retrieval) · `bb4984d` (--min-score fix).
