---
title: "<Project> — Open Items"
type: open-items
project: <project>
updated: YYYY-MM-DD
---

# Open Items — <Project>

> Project-scoped forward todos. **`/vault-push` owns this file** — add / tick / carry every session.
> Parallel-safe because it's project-scoped (one writer), like `PROJECT_LOG.md`.
> Cross-cutting / ops todos go to the global `System/dashboards/Open Items.md` instead.
> `- [ ]` open · `- [x]` done. Newest/active at top; move finished items to `## Done`.

## Open
- [ ] <item> — <why / context> · added YYYY-MM-DD

## Done
- [x] <item> — done YYYY-MM-DD
